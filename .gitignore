node_modules/
dist/
build/
.env
!.env.example
*.db
*.db-journal
apps/api/prisma/dev.db
apps/api/uploads/
coverage/
.DS_Store
apps/web/test-results/
apps/web/playwright-report/
apps/web/e2e/.cache/

# Internal docs kept locally for reference but not published to GitHub — see README.md, which
# stays tracked as the repo's landing page. Untracked with `git rm --cached`, not deleted from
# disk; these paths just stop new changes from being picked back up by a future `git add`.
/*.md
!/README.md
